# Build for Cloudflare Tunnel (same-origin via Caddy :8080).
# Relative API works for any *.trycloudflare.com hostname without rebuild.
VITE_API_URL=/api/v1
VITE_API_BASE_URL=/api/v1
VITE_ENVIRONMENT=DEMO
